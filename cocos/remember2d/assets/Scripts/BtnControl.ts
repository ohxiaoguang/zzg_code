const {ccclass, property} = cc._decorator;

@ccclass
export default class BtnControl extends cc.Component {

    // @property(script)
    // screen:script = null;

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {}

    // start () {
    //     this.node.on('click', this.callback, this);
    // }
    // callback(button:cc.Button){
    //     console.log(button.getComponentInChildren(cc.Label).string)
    //     this.screen.click1();
    // }

    // update (dt) {}
}
